# PyTorch must be installed separately — it needs a platform-specific index URL.
# Pinned to 2.6.0 (released 2026-03-23); torchaudio 2.12.0 does not yet exist.
#
#   Apple Silicon (arm64, PyPI wheels):
#     pip install torch==2.6.0 torchaudio==2.6.0
#
#   Docker CPU (pytorch.org CPU wheel index):
#     pip install torch==2.6.0 torchaudio==2.6.0 --index-url https://download.pytorch.org/whl/cpu
#
#   Docker CUDA 12.4:
#     pip install torch==2.6.0 torchaudio==2.6.0 --index-url https://download.pytorch.org/whl/cu124
#
# Then install the rest of the dependencies:
#   pip install -r requirements.txt
transformers==5.12.0
accelerate==1.14.0
safetensors==0.8.0
huggingface-hub==1.19.0
soundfile==0.14.0
datasets==5.0.0
fastapi==0.137.1
uvicorn[standard]==0.49.0
python-multipart==0.0.32
httpx==0.28.1
