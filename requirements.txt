# Install PyTorch separately first (no --index-url on Apple Silicon arm64):
#   pip install torch torchvision torchaudio
# Then install the rest:
#   pip install -r requirements.txt
transformers>=4.52.1
accelerate
safetensors
huggingface-hub
soundfile
datasets
fastapi
uvicorn[standard]
python-multipart
httpx
